# 🚀 ColorToMusic 快速啟動指南

## 立即開始使用

### 方法一：直接開啟（最簡單）
1. 在檔案總管中找到 `index.html`
2. 雙擊檔案，會自動在預設瀏覽器中開啟
3. 開始上傳圖片並體驗音樂轉換！

### 方法二：使用本地伺服器（推薦）
為了獲得最佳體驗，建議使用本地伺服器：

#### 使用 Python (如果已安裝)
```bash
# 在專案資料夾中開啟終端機
cd c:\_Git\ColorToMusic

# Python 3
python -m http.server 8000

# Python 2
python -m SimpleHTTPServer 8000
```

#### 使用 Node.js (如果已安裝)
```bash
# 安裝 http-server (只需執行一次)
npm install -g http-server

# 在專案資料夾中執行
cd c:\_Git\ColorToMusic
http-server
```

#### 使用 VS Code Live Server
1. 在 VS Code 中開啟專案資料夾
2. 安裝 "Live Server" 擴充套件
3. 右鍵點擊 `index.html` → "Open with Live Server"

然後在瀏覽器中開啟顯示的網址（通常是 `http://localhost:8000`）

## 🎯 快速測試

1. **測試範例圖片**：點擊主頁面標題下方的「🧪 測試範例圖片」連結
2. **下載測試圖片**：在測試頁面中點擊任一測試圖片進行下載
3. **上傳並轉換**：回到主頁面，上傳剛下載的測試圖片
4. **調整參數**：嘗試不同的掃描方向和映射方式
5. **產生音樂**：點擊「產生音樂」按鈕
6. **播放體驗**：使用播放按鈕聆聽您的圖片音樂！

## 💡 使用技巧

- **小圖片**：處理速度快，適合快速測試
- **大圖片**：增加「像素跳躍」數值以加快處理
- **黑白圖片**：建議使用「亮度」映射模式
- **彩色圖片**：建議使用「鮮豔度」映射模式
- **快捷鍵**：空白鍵播放/停止，Ctrl+Enter 產生音樂

## ❓ 常見問題

**Q: 為什麼沒有聲音？**
A: 確保瀏覽器允許音頻播放，某些瀏覽器需要使用者互動才能播放音頻。

**Q: 處理大圖片很慢怎麼辦？**
A: 增加「像素跳躍」數值，或縮短「音樂長度」。

**Q: 音樂聽起來很吵雜？**
A: 嘗試調整頻率範圍，或使用不同的映射方式。

**Q: 支援哪些瀏覽器？**
A: Chrome、Firefox、Safari、Edge 的現代版本都支援。

## 🎉 盡情探索！

現在您已經準備好探索圖片與音樂的奇妙結合了！試試不同的圖片、參數組合，創造出屬於您的獨特音樂作品。