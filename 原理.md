# ColorToMusic 完整技術原理與使用說明

我來詳細說明 ColorToMusic 的轉換原理！這個系統將圖片的視覺資訊轉換為聽覺音樂，涉及多個步驟的數學轉換。

## 📋 目錄
- [色彩轉換原理](#🎨-色彩轉換原理)
- [掃描順序原理](#🔄-掃描順序原理)  
- [音頻轉換原理](#🎵-音頻轉換原理)
- [GUI 控制項完整說明](#🎛️-GUI-控制項完整說明)
- [像素處理策略](#📊-像素處理策略)
- [音符連貫性系統](#🎼-音符連貫性系統)
- [音高限制系統](#🎹-音高限制系統)
- [效能最佳化原理](#📊-效能最佳化原理)
- [技術規格](#🔬-技術規格)
- [實際應用範例](#🎨-實際應用範例)

## 🎨 色彩轉換原理

### 1. RGB 像素讀取
每個圖片像素包含 4 個數值：
- **R (Red)**：紅色強度 (0-255)
- **G (Green)**：綠色強度 (0-255)  
- **B (Blue)**：藍色強度 (0-255)
- **A (Alpha)**：透明度 (0-255)

### 2. 色彩數值化 - 兩種映射方式

#### 方式一：亮度映射 (Brightness)
使用**感知亮度公式**，模擬人眼對不同色彩的亮度感知：

```javascript
brightness = 0.2126 × R + 0.7152 × G + 0.0722 × B
```

**原理解釋：**
- 人眼對綠光最敏感 (70.52%)
- 對紅光中等敏感 (21.26%)  
- 對藍光最不敏感 (7.22%)
- 這個公式符合 ITU-R BT.709 標準

**適用場景：**
- 黑白照片
- 明暗對比強烈的圖片
- 需要平滑音調變化的場合

#### 方式二：鮮豔度映射 (Vividness)
使用**色彩距離公式**，計算顏色偏離灰色軸的程度：

```javascript
vividness = √((R-G)² + (G-B)² + (B-R)²)
```

**原理解釋：**
- 當 R=G=B 時，vividness = 0 (灰色)
- 色彩差異越大，數值越高
- 最大值約為 441 (純色對比)

**適用場景：**
- 彩色豐富的圖片
- 需要突出色彩變化的場合
- 抽象藝術或設計圖片

## 🔄 掃描順序原理

### 1. 左到右掃描 (left-right)
```javascript
for (let y = 0; y < height; y++) {
    for (let x = 0; x < width; x++) {
        scanPixel(x, y);
    }
}
```
- **音樂特點**：橫向的音調流動
- **適合**：風景照、全景圖

### 2. 上到下掃描 (top-down)
```javascript
for (let x = 0; x < width; x++) {
    for (let y = 0; y < height; y++) {
        scanPixel(x, y);
    }
}
```
- **音樂特點**：縱向的音調變化
- **適合**：建築物、垂直構圖

### 3. 對角線掃描 (diagonal)
```javascript
for (let d = 0; d < maxDiagonal; d++) {
    for (let x = 0; x < width; x++) {
        const y = d - x;
        if (y >= 0 && y < height) {
            scanPixel(x, y);
        }
    }
}
```
- **音樂特點**：螺旋式音調發展
- **適合**：抽象圖片、創意效果

## 🎵 音頻轉換原理

### 1. 數值到頻率映射
使用**線性映射**將色彩數值轉換為音頻頻率：

```javascript
frequency = minFreq + (value - minValue) / (maxValue - minValue) × (maxFreq - minFreq)
```

**映射過程：**
1. **正規化**：`(value - minValue) / (maxValue - minValue)` → 0-1 範圍
2. **縮放**：乘以頻率範圍 `(maxFreq - minFreq)`
3. **偏移**：加上最小頻率 `minFreq`

**範例：**
- 色彩數值：128 (範圍 0-255)
- 頻率範圍：220Hz - 1760Hz  
- 計算：220 + (128/255) × (1760-220) = 220 + 0.502 × 1540 = 993Hz

### 2. 正弦波產生
使用**正弦波公式**產生純音：

```javascript
sample = Math.sin(2π × frequency × time) × volume × envelope
```

**參數說明：**
- **frequency**：音高 (Hz)
- **time**：時間點 (秒)
- **volume**：基礎音量 (0.3)
- **envelope**：包絡線 (淡入淡出效果)

### 3. 包絡線設計
避免音頻爆音的淡入淡出效果：

```javascript
const fadeIn = Math.min(1, time × 10);           // 0.1秒淡入
const fadeOut = Math.min(1, (duration - time) × 10); // 0.1秒淡出
const envelope = Math.min(fadeIn, fadeOut);
```

## 📊 效能最佳化原理

### 1. 像素跳躍 (Pixel Skip)
```javascript
if (pixelCount % pixelSkip !== 0) {
    pixelCount++;
    return; // 跳過此像素
}
```
- **目的**：減少處理的像素數量
- **效果**：加快處理速度，縮短音樂長度

### 2. 自動調整保護
```javascript
const maxPixels = 50000;
if (totalPixels > maxPixels) {
    adjustedPixelSkip = Math.ceil(totalPixels / maxPixels);
}
```
- **保護機制**：防止大圖片造成效能問題
- **自動優化**：維持合理的處理時間

## 🎶 音樂合成原理

### 1. 音符時序計算
```javascript
const actualNoteDuration = duration / totalNotes;
const startTime = i × actualNoteDuration;
```

### 2. 多音符混合
```javascript
for (const note of notes) {
    const startSample = Math.floor(note.startTime × sampleRate);
    // 將每個音符加入主緩衝區
    channelData[startSample + i] += noteSamples[i];
}
```

### 3. 音量正規化
```javascript
if (maxAmplitude > 0.8) {
    const scale = 0.8 / maxAmplitude;
    for (let i = 0; i < channelData.length; i++) {
        channelData[i] *= scale;
    }
}
```

## 🔬 技術規格

- **採樣率**：44,100 Hz (CD 品質)
- **位元深度**：32-bit Float (內部處理)
- **匯出格式**：16-bit WAV
- **音量範圍**：-1.0 到 +1.0

## 🎨 實際應用範例

假設有一個 **3×3 像素的彩虹圖片**：

```
[紅][綠][藍]
[黃][白][紫]  
[橙][灰][黑]
```

**轉換步驟：**

1. **RGB 讀取**：
   - 紅色：(255,0,0) → 亮度=54, 鮮豔度=180
   - 綠色：(0,255,0) → 亮度=183, 鮮豔度=180
   - 藍色：(0,0,255) → 亮度=18, 鮮豔度=180

2. **頻率映射** (使用亮度模式，220-1760Hz)：
   - 紅色：54 → 545Hz (低音)
   - 綠色：183 → 1315Hz (高音)  
   - 藍色：18 → 330Hz (極低音)

3. **音樂產生**：
   - 0.0-1.1秒：545Hz 正弦波
   - 1.1-2.2秒：1315Hz 正弦波
   - 2.2-3.3秒：330Hz 正弦波

這樣就完成了從視覺到聽覺的完整轉換！🎵

---

## 🎛️ GUI 控制項完整說明

### 1. 頻率範圍
- **低音/高音滑桿**：設定音樂最低/最高頻率（如 220Hz~1760Hz）
- 影響音樂的整體音高分布

### 2. 音樂長度
- **音樂長度 (秒)**：設定整首音樂的總時長

### 3. 像素處理方式
- **像素跳躍**：每 N 個像素取 1 個，快速但可能丟失細節
- **像素平均**：每 N 個像素取平均值，保留更多資訊，品質較高
- **全部處理**：處理所有像素，最高品質但速度較慢（建議小圖片使用）

### 4. 音符長度
- 控制每個音符的持續時間，越長越悠揚

### 5. 休止符比例
- 在音符間加入靜默，減少急促感

### 6. 音符分組
- **無分組**：每個像素對應一個音符
- **簡單分組**：每兩個像素合併，減少音符數
- **智慧分組**：合併相似色彩像素，大幅減少音符數

### 7. 音符連貫性
- **離散**：每個音符獨立，節奏感強
- **連奏**：音符重疊，平滑過渡
- **滑音**：頻率連續滑動，創造流暢音調

### 8. 音高限制
- **無限制**：使用完整頻率範圍，可能產生極高或極低音
- **音樂限制**：限制在 C3-C6 (130-1047 Hz)，舒適聽覺範圍
- **自訂限制**：手動設定範圍，超出範圍的音符會八度轉置

---

## 📊 像素處理策略
- **像素跳躍**：加快處理速度，適合大圖片
- **像素平均**：提升音樂品質，平衡效率與細節
- **全部處理**：最高品質，僅建議小圖片

---

## 🎼 音符連貫性系統
- **離散音符**：每個音符獨立，清晰分離
- **連奏**：音符重疊，平滑過渡
- **滑音**：頻率連續變化，如小提琴效果

---

## 🎹 音高限制系統
- **無限制**：完整頻率範圍
- **音樂限制**：C3-C6 (130-1047 Hz)，舒適聽覺
- **自訂限制**：手動設定範圍，超出範圍的音符會八度轉置，保持音樂的相對關係

---

> 所有設定都可即時調整，並會在預估音符數量、音樂預覽、音高範圍等資訊區域即時反映。